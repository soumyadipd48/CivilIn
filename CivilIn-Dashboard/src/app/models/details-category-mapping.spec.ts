import { DetailsCategoryMapping } from './details-category-mapping';

describe('DetailsCategoryMapping', () => {
  it('should create an instance', () => {
    expect(new DetailsCategoryMapping()).toBeTruthy();
  });
});
